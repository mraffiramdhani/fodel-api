APP_PORT=4040
API_VERSION=1
APP_KEY=y0u12_4pp_k3y

DB_SERVER=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=secret
DB_DATABASE=fodel
DB_SSLMODE=disable
DB_MAX_CONNECTIONS=5

# Preferred: use DATABASE_URL directly
# Local Postgres:
# DATABASE_URL=postgresql://postgres:secret@127.0.0.1:5432/fodel?sslmode=disable
# Supabase:
# DATABASE_URL=postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require

MAIL_HOST=smtp.gmail.com
MAIL_USER=your_email@gmail.com
MAIL_PASS=your_email_password

# Optional: run SQL seed script on startup
RUN_SQL_SEED=false

# Optional: disable startup migrations (recommended when using Supabase transaction pooler)
RUN_MIGRATIONS=true
